# Модуль 08 — Структурные паттерны: Декоратор и Адаптер

## 📘 Описание проекта
Данный проект выполнен в рамках курса **"Шаблоны проектирования приложений"**  
и демонстрирует работу двух структурных паттернов:
- **Декоратор (Decorator)**
- **Адаптер (Adapter)**

---

## ☕ Паттерн "Декоратор"

**Задача:**  
Разработать систему управления заказами в кафе, где к базовому напитку (например, кофе, чай) можно добавлять различные добавки (молоко, сахар, сливки, сироп и т.д.).  

**Решение:**  
Использован паттерн "Декоратор", который позволяет динамически добавлять новые "украшения" (добавки) к напитку без изменения его базовой структуры.

**Классы:**
- `Beverage` — абстрактный базовый класс напитка  
- `Espresso`, `Tea`, `Latte`, `Mocha` — конкретные напитки  
- `BeverageDecorator` — абстрактный класс-декоратор  
- `Milk`, `Sugar`, `WhippedCream`, `VanillaSyrup` — конкретные декораторы

**Результат:**  
Можно гибко комбинировать напитки и добавки, а программа корректно рассчитывает итоговую стоимость.

---

## 💳 Паттерн "Адаптер"

**Задача:**  
Интегрировать сторонние системы оплаты в существующую систему интернет-магазина,  
используя общий интерфейс `IPaymentProcessor`.

**Решение:**  
Реализованы адаптеры, которые делают возможным использование разных систем оплаты (PayPal, Stripe, FastPay) через единый интерфейс.

**Классы:**
- `IPaymentProcessor` — общий интерфейс для всех систем оплаты  
- `PayPalPaymentProcessor` — реализация существующей системы  
- `StripePaymentService` + `StripePaymentAdapter` — сторонняя система + адаптер  
- `FastPayService` + `FastPayAdapter` — дополнительный пример интеграции

**Результат:**  
Клиентский код (`Program.cs`) может работать с любыми процессорами через один интерфейс, не зная деталей реализации.

---

## 🚀 Запуск проекта

**Требования:**  
Установленный [.NET 7 SDK](https://dotnet.microsoft.com/en-us/download)

**Команды для запуска:**
```bash
cd DesignPatternsDemo
dotnet run
